# aberdeen-traffic-collisions-heatmap
Plot a heatmap of traffic accidents using the datasets provided by ODIaberdeen.

### To Compile
```ng build --prod --aot --no-sourcemaps --buildOptimizer -cc -vc -dop```
